## 2024-05-19 - Extracted ReactMarkdown Components\n**Learning:** Defining react-markdown custom components statically outside the component block prevents unnecessary unmounting and remounting on every render, which is a significant performance anti-pattern. Micro-benchmarks in this environment demonstrate that static object/function references are ~50-60x faster than inline recreation.\n**Action:** Extract 'components' prop object for ReactMarkdown to a module-level constant with proper types using Components from 'react-markdown'.
