## 2024-04-25 - Extracted ReactMarkdown Components\n**Learning:** Shadowed/duplicate core files (e.g. root `PersonaMenu.tsx`, root `geminiService.ts`, `components/App.tsx`) exist and cause `tsc --noEmit` errors.\n**Action:** Remember that these shadowed duplicate components are known anti-patterns in this codebase but I shouldn't delete them if they aren't part of my task. Only verify the main build for the core application.
